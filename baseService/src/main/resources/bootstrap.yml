spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: dev
        group: DEFAULT_GROUP
  application:
    name: baseService
  config:
    import:
      - optional:nacos:baseService.yaml
      - optional:nacos:common.yaml